import { DYNAMIC_VC_CATEGORY, DYNAMIC_VC_JOIN_CHANNEL } from "config";
import { DynamicVcManager } from "./dynamic-vc";

export const manager1 = new DynamicVcManager(DYNAMIC_VC_CATEGORY, DYNAMIC_VC_JOIN_CHANNEL);